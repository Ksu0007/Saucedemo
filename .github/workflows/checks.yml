name: run tests

on:
 push:
  branches: "main"

jobs:
 run-tests:
  runs-on: ubuntu-latest

  steps:
   - name: Checkout
     uses: actions/checkout@v4.1.2

   - name: run tests
     run: mvn test
   
  
